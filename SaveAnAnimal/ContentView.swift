//
//  ContentView.swift
//  SaveAnAnimal
//
//  Created by Lisa Tang on 7/15/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {

            NavigationStack{
                VStack{
                    Text("Save An Animal")
                        .font(.largeTitle)
                        .fontWeight(.bold)
                    Spacer()
                        .frame(height: 3.0)
                    

                                .fontWeight(.bold)
                                .foregroundColor(Color.black)
                        }
                        Spacer()

                                .fontWeight(.bold)
                                .foregroundColor(Color.black)
                        }
                        Spacer()

                                .fontWeight(.bold)
                                .foregroundColor(Color.black)
                        }

                }
        

        


#Preview {
    ContentView()
}
